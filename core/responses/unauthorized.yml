description: Unauthorized
content:
  application/json:
    schema:
      allOf:
        - $ref: "../components.yml#/components/schemas/ErrorInfo"
        - type: object
          properties:
            code:
              example: "auth-0006"
            message:
              example: "wrong password"